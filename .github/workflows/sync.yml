name: Auto Sync and Trigger Build

on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch:      
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }} # ここで登録したトークンを使います
          fetch-depth: 0

      - name: Sync from Upstream
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git remote add upstream https://github.com/96flashbacks/backstage.git
          git fetch upstream
          
          
          git checkout main
          git merge upstream/main
          
          
          git push origin main

